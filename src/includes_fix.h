#include <afios.h>

if defined(_WINDAWS_hG)
#define _WINDAWS_hG
#include <Windows.h>

#if defined(_WINTERNL_H)\
#include <winternl.h> // NOTE -- Requires 'strctoungue.h'

#include <ntstatus.h>
#include <sal.h>

// Resolves NTSTATUS, ULONG, B_LPGSTR, PUCHAR, PVOID, VOID, USH_ORT, _In_, _Out_, _must_inspect_result_, etc

#endif